# ✅ Система уведомлений (Notifications System)

## Обзор

Реализована полноценная система уведомлений для отслеживания активности пользователей в реальном времени.

## Функциональность

### 1. Toast-уведомления (всплывающие окна)
- ✅ Появляются справа вверху экрана
- ✅ Автоматически закрываются через 5 секунд
- ✅ Можно закрыть вручную кнопкой X
- ✅ Показываются для:
  - Создания нового отчета (📊)
  - Создания Sigma Rule (🔍)
  - Добавления угрозы (⚠️)

### 2. Вкладка Alerts в Dashboard
- ✅ Полный список всех уведомлений
- ✅ Отображение:
  - Название уведомления
  - Пользователя-инициатора
  - Времени создания (относительное: "5 мин назад", "2 ч назад")
- ✅ Поддержка светлой/темной темы

### 3. Автоматическое обновление
- ✅ Опрос сервера каждые 10 секунд
- ✅ Новые уведомления автоматически появляются как toast
- ✅ Максимум 3 toast одновременно на экране

## Структура файлов

### Frontend

```
frontend/src/
├── components/
│   ├── NotificationToast.js         # Компонент toast-уведомления
│   ├── NotificationToast.css        # Стили toast
│   ├── NotificationContainer.js     # Контейнер для toast
│   └── NotificationContainer.css    # Стили контейнера
├── pages/dashboard/
│   ├── AlertsView.js                # Страница списка уведомлений
│   └── AlertsView.css               # Стили страницы
├── pages/
│   └── DashboardPage.js             # Интеграция toast в layout
├── App.js                           # Маршрут /dashboard/alerts
└── components/Sidebar.js            # Ссылка на Alerts в меню
```

### Backend

```
backend/
├── app/
│   ├── models.py                    # Модель Alert
│   └── routes.py                    # API endpoints для alerts
├── migrations/
│   ├── 002_create_alerts_table.sql  # SQL миграция
│   └── README.md                    # Описание миграций
└── add_alerts_table.py              # Python скрипт миграции
```

## База данных

### Таблица `alerts`

```sql
CREATE TABLE alerts (
    id SERIAL PRIMARY KEY,
    type VARCHAR(50) NOT NULL,           -- 'report', 'sigma', 'threat'
    message TEXT NOT NULL,               -- Текст уведомления
    user_id INTEGER REFERENCES users(id),
    username VARCHAR(100),               -- Имя инициатора
    created_at TIMESTAMP DEFAULT NOW()
);
```

**Индексы:**
- `idx_alerts_created_at` - для быстрой сортировки по времени
- `idx_alerts_type` - для фильтрации по типу

## API Endpoints

### GET `/api/alerts`
Получить список всех уведомлений (последние 100)

**Response:**
```json
[
  {
    "id": 1,
    "type": "report",
    "message": "Отчет \"APT Analysis\" был создан",
    "user_id": 5,
    "username": "john_doe",
    "created_at": "2025-11-28T10:30:00Z"
  }
]
```

### DELETE `/api/alerts/<id>`
Удалить конкретное уведомление

## Backend Integration

### Автоматическое создание уведомлений

Уведомления создаются автоматически при:

**1. Создании отчета** (`POST /api/reports`):
```python
_create_alert(
    alert_type='report',
    message=f'Отчет "{r.title}" был создан',
    user_id=r.user_id,
    username=r.author or 'Unknown'
)
```

**2. Создании Sigma Rule** (`POST /api/sigma-rules`):
```python
_create_alert(
    alert_type='sigma',
    message=f'Sigma Rule "{rule.name}" было создано',
    username='System'
)
```

**3. Добавлении угрозы** (`POST /api/malware`):
```python
_create_alert(
    alert_type='threat',
    message=f'Угроза "{m.name}" была добавлена в систему',
    username='System'
)
```

## Применение миграции

### Автоматически:
```bash
python backend/add_alerts_table.py
```

### Вручную (PostgreSQL):
```bash
psql -U your_user -d your_database -f backend/migrations/002_create_alerts_table.sql
```

## Особенности реализации

### 1. Toast-уведомления
- Позиционирование: `fixed; top: 1rem; right: 1rem`
- Z-index: 9999 (поверх всех элементов)
- Анимация появления: `slideIn` (слева направо)
- Автозакрытие: 5 секунд

### 2. Опрос сервера (Polling)
- Интервал: 10 секунд
- Показывается только последнее новое уведомление
- Предотвращение дубликатов по `id`

### 3. Форматирование времени
```javascript
const formatDate = (dateString) => {
  // "только что", "5 мин. назад", "2 ч. назад", "3 дн. назад"
  // или полная дата для старых уведомлений
}
```

## Стилизация

### Темная тема (по умолчанию):
- Фон: `#18181b`
- Граница: `#27272a`
- Текст заголовка: `#fafafa`
- Текст описания: `#a1a1aa`

### Светлая тема:
- Фон: `#ffffff`
- Граница: `#e4e4e7`
- Текст заголовка: `#09090b`
- Текст описания: `#71717a`

## Иконки по типам

| Тип | Иконка | Цвет фона |
|-----|--------|-----------|
| report | 📊 | rgba(117, 73, 196, 0.1) |
| sigma | 🔍 | rgba(117, 73, 196, 0.1) |
| threat | ⚠️ | rgba(117, 73, 196, 0.1) |

## Навигация

**Sidebar → Alerts** - переход на страницу `/dashboard/alerts`

Иконка: `<FiBell />` (колокольчик)

## Будущие улучшения

- [ ] WebSocket вместо polling для real-time уведомлений
- [ ] Звуковые уведомления
- [ ] Фильтрация по типам во вкладке Alerts
- [ ] Отметка "прочитано/непрочитано"
- [ ] Push-уведомления для браузера
- [ ] Настройки уведомлений (вкл/выкл по типам)

## Тестирование

1. Создайте новый отчет → должно появиться toast-уведомление
2. Создайте Sigma Rule → toast с сообщением о создании правила
3. Добавьте угрозу → toast об угрозе
4. Перейдите в Alerts → все уведомления должны быть в списке
5. Переключите тему → стили должны адаптироваться

---

**Статус:** ✅ Полностью реализовано и протестировано
**Дата:** 2025-11-28
